# Evidence Registry

This folder stores documentation-side indexes/templates for immutable execution evidence.

Recommended subdomains:
- observed replay
- API probe
- crawl release
- semantic QA
- NCS audit
- mart audit
- publication release

Large runtime artifacts remain outside Git; the docs package stores stable path/ID/SHA pointers.
